# Tipe data `tuple`  digunakan untuk menyimpan sejumlah nilai yang tidak dapat diubah setelah tuple tersebut dibuat (immutable).
# Berikut adalah beberapa karakteristik-nya :
"""
1. Tidak Dapat Diubah (Immutable):
    Setelah elemen-elemen ditetapkan ke dalam tuple, tuple tersebut tidak dapat diubah.
2. Urutan (Ordered):
    Elemen-elemen dalam tuple memiliki urutan tertentu, yang berarti kita dapat merujuk pada elemen berdasarkan indeksnya.
3. Dinyatakan dengan Tanda Kurung Biasa `( )`:
    Tuple dideklarasikan dengan menggunakan tanda kurung biasa `( )`.
4. Dapat Menyimpan Berbagai Tipe Data:
    Seperti list, tuple juga dapat menyimpan elemen-elemen dengan berbagai tipe data.
"""
# membuat tuple
x = (1, "Programing", 1+3j, True, 200, "jakarta")
print(type(x))
print(x)

# Mengakses elemen berdasarkan indeks
print(x[2])

# Mengetahui Panjang dari tuple
panjang_x = len(x)
print(panjang_x)
